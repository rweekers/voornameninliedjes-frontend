'use strict';

describe('myApp.suggestion module', function() {

  beforeEach(module('myApp.suggestion'));

  describe('suggestion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var suggestionCtrl = $controller('SuggestionCtrl');
      expect(suggestionCtrl).toBeDefined();
    }));

  });
});